## Question 1

1. No the array doesnt have to be filled in sequentially from the 0th index, because we order the array at the end when we are printing.

2. Because we did not fill the array at [3][1]

4. The audience.length is to print the length of the row of the array, meanwhile the audience[0].length is to print the length of the column of the array. The audience[0].length, audience[1].length, audince[2].length, audience[3].length have the same length because all of the column in the array have the same number of column

7. Foreach loop is better beacuse it is simpler but the disadvantages is ther is no index access which mean you cant access a specific location of the array

8. The max row index for the audience array is 4

9. The max column index for the audience array is 2

13. The function of the String,join() is to concatenates multiple strings into a single string

## Question 2

1. The array is filled from the oth index, but because we add row -1 column - 1 we filled the row and column starting 1

## Question 3

2. it will produce a string representation of the array

3. The default value is 0

4. To make the length of each row the same we need to set the row length in the int[][] myNumbers = new int[3][];