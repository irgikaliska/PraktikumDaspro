## Question 1

1. If there is no value that we want to return we use void.

2. No we can't.

3. To make it more readable and easier to test.

4. First we declare the function in the file class and wrote all the program that we want to output. and then execute the function in the main function inside the class by writing the function name.

## Question 2

1. Paramater is used to pass the values into the functions so that it can be used 

2. Because the function needs to know what the name of the customer and are they a member or not

3. It is similar parameter is used to receive value when the function is called

4. If isMember is true, the program displays the member discount information. If false, the section is not displayed

5. The program will not be able to ran because the function need the parameter

## Question 3

1. A function need a return value when the result is used again if the function only produced an output and doesnt need to be used again we can use void to not return any value from the function

2. The return data type is int, the int pilihanMenu is for the kind of food the customer want, the int banyakItem is for the number of food that the customer want to order

## Question 4

1. To be able to accept a varying number of arguments of the same data type

3. No, you cant use two different data type in varargs, only one data type is allowed in java

4. The program will still run but there will be no data that shown

## Question 5 

1. First we declare the length, width, height, area, and volume. next input the data and then call the function to calculate the area and volume. lastly print the calculated data

2. When we input length as 4, width as 3, and height as 5 and the we called the function while putting the variable in the parameter.after we get the calculated data back, we print it to display it

3. The output is 1234567. First we call the jumlah function and assign it to temp, next we call the tampiljumlah function and we put temp and 5 as the bil1 and bil2. we then call the jumlah again in the function and put it in the tampilhinggakei, that function will use a loop to display the number

4. If we want to use the data in the function then we need to use parameter and declare the datatype, if we dont want to use the data in the function then we can use void to get no return value from the function

5. A function need a parameter if the function need to calculate something from an input, if we input a data into a variable we can put it in the parameter we use in the function to calculate it. If we dont need a data from an input we can use no paramater

6. A function need to have a return if we want to use the data we get from the function if we only need the output then we dont need a return